package shape;

public class Triangle<T extends Number> extends Shape {
  @Override
  public double area() {
      // Implement the area calculation formula for a triangle
      // For example: return (base * height) / 2.0;
      // Replace with the actual formula based on your requirements
      return 0.0; // Placeholder value, replace with actual calculation
  }
}
